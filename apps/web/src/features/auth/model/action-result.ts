/** Схема не сошлась на сервере: форма прислала то, чего браузер бы не пропустил. */
export const INVALID_INPUT_MESSAGE = 'Проверьте правильность заполнения полей';

/** Экшены не бросают исключения наружу: неудачу возвращают этим объектом. */
export interface AuthActionError {
  error: string;
  /** Заполнено, когда виновато конкретное поле — форма повесит ошибку на него. */
  field?: 'email';
}
